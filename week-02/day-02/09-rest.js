`use strict`
//before ES6
function extractArgumentsInES5() {
  if(arguments.length < 3 || arguments.forEach(item => {
    typeof item !== Number;
  })) {
    return null;
  }
  var rows = arguments[0];
  var cols = arguments[1];
  var matrix = [];
  for (var i = 0; i < rows; i++) {
    for (var j = 2; j < arguments.length - 2; j++) {
      
    }
  }
  Array.prototype.shift.call(arguments);
  Array.prototype.shift.call(arguments);
}